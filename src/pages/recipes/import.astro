---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ImportLoader from '../../components/ImportLoader.tsx';
import { getUser } from '../../lib/session';

const { SESSION_SECRET, IXIL_PASSWORD, MATHILDE_PASSWORD } = Astro.locals.runtime.env;
const env = { SESSION_SECRET, IXIL_PASSWORD, MATHILDE_PASSWORD };
const user = await getUser(Astro.request, env);
if (!user) return Astro.redirect('/login');
---

<BaseLayout title="Import Recipe — Lunchbox">
  <h1 class="page-heading">Import from URL</h1>
  <ImportLoader client:load currentUser={user} />
</BaseLayout>
